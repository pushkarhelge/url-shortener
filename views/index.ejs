<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Document</title>
    <style>
        .footer {
            padding-top: 1rem;
        }

        .footer__copy {
            font-size: var(--smaller-font-size);
            text-align: center;
            /* color: var(--text-color-light); */
            color: rgb(5, 3, 0);
            margin-top: var(--mb-3);
        }
    </style>
</head>

<body>
    <div class="container">

        <h1  class="my-2">URL Shrinker</h1>
        <form action="/shortUrls" method="post" class="my-4 form-inline">
            <label for="fullUrl" class="">Url</label>
            <input required placeholder="Enter your URL here" type="url" name="fullUrl" id="fullUrl"
                class="form-control mr-2">
            <button class="btn btn-success my-2" type="submit">Shrink</button>
        </form>

        <table class="table table-striped table-responsive">
            <thead>
                <th>Full URL</th>
                <th>Short URL</th>
                <th>Clicks</th>
            </thead>
            <tbody>
                <% shortUrls.forEach(shortUrl=>{ %>
                    <tr>
                        <td><a href="<%= shortUrl.full %>" target="_blank">
                                <%= shortUrl.full %>
                            </a></td>
                        <td><a href="<%= shortUrl.short %>" target="_blank">
                                <%= shortUrl.short %>
                            </a></td>
                        <td><a href="<%= shortUrl.clicks %>" target="_blank">
                                <%= shortUrl.clicks %>
                            </a></td>
                    </tr>
                    <%}) %>
            </tbody>
        </table>
        <footer class="footer">
            <p class="footer__copy">&#169; Pushkar Helge, All rights reserved.</p>
        </footer>
    </div>
</body>

</html>